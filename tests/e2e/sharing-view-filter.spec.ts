import { test } from '@playwright/test';

test.describe('Sharing View Filter', () => {
  test('Sharing view only shows entries for the requested contest', async () => {
    // Placeholder test
    
    // Setup mock entries for semi1, semi2, and final
    
    // await page.goto('/share/semi1/user-id');
    
    // Verify only semi1 entries are visible
    // const entries = await page.locator('.entry-item').allTextContents();
    // expect(entries.length).toBeGreaterThan(0);
    // for (const text of entries) {
    //   expect(text).not.toContain('Semi2 Country');
    //   expect(text).not.toContain('Final Only Country');
    // }
  });
});
