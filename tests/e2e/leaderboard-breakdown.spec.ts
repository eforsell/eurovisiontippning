import { test } from '@playwright/test';

test.describe('Leaderboard Breakdown', () => {
  test('expanding a row displays individual contest scores', async () => {
    // Placeholder test
    
    // await page.goto('/sharing/leaderboard');
    
    // Click on the first row to expand it
    // await page.locator('.leaderboard-row').first().click();
    
    // Verify the expanded section shows semi1, semi2, and final scores
    // await expect(page.locator('.score-breakdown')).toBeVisible();
    // await expect(page.locator('.score-semi1')).toContainText('Semi 1:');
  });
});
