import { test } from '@playwright/test';

test.describe('Admin Progression Lock', () => {
  test('Progression action is disabled if contest has not started', async () => {
    // This is a placeholder test that needs to be updated with real selectors and auth logic
    // once the frontend implementation is complete. It assumes the contest is mocked as "not started"
    
    // Setup mock data/state where a contest has NOT started yet
    
    // await page.goto('/admin');
    
    // Verify the progression button is disabled
    // const progressionButton = page.locator('button:has-text("Progress Semifinal")');
    // await expect(progressionButton).toBeDisabled();
  });
});
