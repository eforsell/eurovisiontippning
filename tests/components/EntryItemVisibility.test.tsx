import { describe, it } from 'vitest';
// import { render, screen } from '@testing-library/react';
// import { EntryItem } from '../../src/components/Ranking/EntryItem';

describe('EntryItem Visibility', () => {
  it('should display the score when provided', () => {
    // Placeholder for actual component test
    // render(<EntryItem entry={mockEntry} score={10} />);
    // expect(screen.getByText('10')).toBeInTheDocument();
  });

  it('should apply reduced opacity and dashed border if failed to progress', () => {
    // Placeholder for actual component test
    // const { container } = render(<EntryItem entry={mockEntry} hasFailed={true} />);
    // expect(container.firstChild).toHaveClass('opacity-50');
    // expect(container.firstChild).toHaveClass('border-dashed');
  });
});
